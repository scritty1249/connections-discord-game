<?xml version="1.0"?>
<!DOCTYPE fontconfig SYSTEM "urn:fontconfig:fonts.dtd">
<fontconfig>
    <!-- This is a dummy file so fontconfig library stops complaining. Should be paired with a FONTCONFIG_PATH that points to this file -->
    <dir>/var/task/app/public/fonts</dir>
    <cachedir>/tmp/fonts-cache</cachedir>
    <config></config>
</fontconfig>
